#!/usr/bin/env groovy

pipeline {
    agent any
   stages {
        stage ('Load Groovy Script') {
          git branch: 'master',
              url: 'https://github.com/srflaxu40/JenkinsWorld'

          steps {
              load 'pipelines/add-agent.groovy'
          }
     }

    }
}

